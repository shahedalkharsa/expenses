import ReactDOM from 'react-dom';

import './index.css';
import App from './App'; // you don't need to write .js

ReactDOM.render(<App />, document.getElementById('root')); // root is from public/inde.html
// so replace the html component with react component 

